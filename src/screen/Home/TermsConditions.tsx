import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  View,
  Text,
  TouchableOpacity,
  Dimensions,
  TextInput,
  Image,
  KeyboardAvoidingView,
  Platform,
} from 'react-native';

const TermsConditions = () => {
  return <Text>TermsConditions</Text>;
};

export default TermsConditions;
